# AgentLab Interview V0.1 实施计划
1. 空目录采用 React/TypeScript/Vite/Monaco + FastAPI/Pydantic + SQLite。
2. 完成版本任务包、独立会话、代码快照、Docker 执行与报告闭环，再扩展三题。
3. 单 Agent 使用兼容 Chat Completions 的真实 HTTP 工具循环与显式 mock；提示由服务端递增授权。
4. 完成中文工作区、持久化恢复、历史和证据链接。
5. 运行后端测试、前端构建与验收，记录未执行部分。

重要假设：本地单用户、仅监听回环地址；隐藏检查仅正常应用隔离。用户代码仅在 Docker 内运行。宿主只执行可信 pytest 行为断言，通过 Docker JSON 协议调用用户代码，不采信用户打印的测试结论。当前环境无 Docker，禁止宿主降级。

## V0.1.1：知识库版本污染

1. 新建 rag_versioning/1.0.0：SQLite 追加旧片段故障、标准库确定性检索、公开轨迹与固定行为数据；先用 Docker 验证故障/正常/参考及四种规避。
2. manifest 声明 readable/editable/runtime/private_assets；旧题缺省为单文件。新快照稳定编码路径及内容，含只读入口；旧快照算法不变。多文件保存使用完整文件集合与原子目录切换，运行从固定快照加载。
3. 文件 API、Agent 和文件树复用服务端规则；完整保存再执行。公开诊断轨迹明确非可信判分，隐藏轨迹不返回；报告 diff 汇总全部可编辑文件。
4. 扩展 Docker 和浏览器门禁、旧题回归及多文件/提示/报告隔离验证；真实模型只复用正在运行后端已有配置，不读取 .env 密钥。更新文档，保留历史资产，无 CI/V0.2。

## V0.2 实施计划（2026-09-16）
1. 保留 V0.1.1 未提交工作，新增真实 URL 路由和三个主导航；生成草稿与训练草稿独立保存。
2. 新增 Pydantic 契约/资产协议、持久化阶段状态、调用审计和有限重试。四阶段独立上下文，复用模型适配器；生成模型预算独立于既有报告预算。
3. 固定标准库运行环境，结构化 JSON 输入/期望值由可信解释器比较。全部项目版本只在原 Docker 隔离内运行；验证正常、故障重复、参考和至少两种规避。
4. 人工审核确认冻结摘要后发布到 data/published；生成任务与四道固定题共用训练，不修改历史任务版本。契约修改显式增版并废弃后续验证，不原地修改已发布包。
5. 确定性/真实 Docker/浏览器回归；现有配置下尝试工具副作用、工作流恢复、权限/上下文三个真实需求，至少一例完整浏览器发布训练闭环。记录所有失败，不把 mock 计作真实生成。

## 多 Agent 自动修复（2026-09-18）

具体角色、失败分类、自动修复路由、统一预算、重新生成交互与验收见 [实施方案](docs/MULTI_AGENT_GENERATION_PLAN.md)。roles-v1 已实现独立角色、诊断路由、自动修复、共享预算及明确的新批次确认；不修改已发布资产或旧失败记录。实际结果和限制以 VERIFICATION.md 为准。

## 生成可靠性七项改进

1. 不可变失败归档、结构化诊断及每轮进展证据。
2. 同资产执行复用、无进展策略切换与结束；规避同场景反例；逐项契约语义影响校核。
3. 实际用量/预算分离，作者摘要及既有发布绑定。
4. 确定性、Docker、浏览器回归；原工具/工作流失败真实模型有限复验，保留所有结果，不发布。

## 按用户要求回退前五项（2026-09-19）

移除自动归档、新诊断限制、停滞/缓存策略、额外反例门禁及六维校核；保留第六、七项展示。保护全部旧数据与更早功能，先定向回归，再实际Docker及完整浏览器验收，不发起真实模型付费复验。

## 生成修复协议（2026-09-19）

先对照实际失败链核对故障指纹、目标/回归分类和动作授权。新生成启用 separated-evidence-v2：独立正确性评测与执行前冻结故障断言分开；评测修复用字段补丁；同一明确契约的期望修复不反复调用契约角色；细化调用失败展示。历史生成协议、发布包和用户数据不迁移。验证按定向回归、实际Docker、前端构建/浏览器、受控真实模型顺序执行，不自动发布真实实例。

## 合并契约设计与项目构建（2026-09-19）

新应用记录启用direct-build-v1：project_build一次生成公开规范与全部实现；spec_review独立只看需求/规范逐输出字段审查；批准后自动冻结，取消生成前用户技术契约确认。规范缺项回构建、真实用户目标歧义才提问，评测可退回错误提案。旧角色流程与数据保留。按协议/恢复回归、实际Docker、浏览器完整提交流程与有界真实模型联调验证，不自动发布真实实例。

执行前评测纠错：具体覆盖差异 → 保留失败候选供原评测修正 → 连续两次失败复用独立规范审查。direct-build-v1两次独立行为执行与一致性证据贯穿发布和训练，不迁移历史题。保持现有预算、隔离、审核门禁；真实复测只用于验证原阻塞是否解除，不强行修完所有后续模型质量问题。

2026-09-20：合并为项目构建/评测/教学三角色，保留7个审计阶段和上下文边界。统一角色策略、显示和修复工单，最新矩阵驱动诊断。验证隔离及旧记录兼容，执行一次默认预算内的真实生成，不论结果都分析原因，不自动发布。
